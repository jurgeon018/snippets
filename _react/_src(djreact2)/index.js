// Описание урока 2
// На бекенде переделали 4 отдельные generic views которые отвечают за CRUD в один viewset. Сделали CreateView, UpdateView и DeleteView на бекенде, передали запрос пользователя с фронтенда на бекенд через axios, на фронтенде отобразили вьюхи при помощи готовых шаблонов от AntDesign. 
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(<App/>, document.getElementById('root'));